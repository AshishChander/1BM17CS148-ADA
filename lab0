#include <stdio.h>

int main(int argc, char **argv)
{
	int n,i,max=0;
	printf("enter the no of entries");
	scanf("%d",&n);
	int a[n];
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
		if(a[i]>max)
			max=a[i];
	}
	printf("max value= %d\n",max);
}
